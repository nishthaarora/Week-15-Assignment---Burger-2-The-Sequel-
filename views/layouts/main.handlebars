<!--
llHere is the main HTML where handlebars will render to
 -->

<!DOCTYPE html>
<html lang="en">
	<head>
		<link rel="stylesheet" href="/assets/css/style.css" type="text/css" />
		<title>Assignment 15!</title>
	</head>
	<body>
	<img src="/assets/images/burger.jpg">
		<p>
		{{#unless logged_in}}
			<button><a href="/users/sign-in">Sign in</a></button>
			<button><a href="/users/new">Sign Up</a></button>
		{{/unless}}

		{{#if logged_in}}
			Welcome {{email}}! <br>
			<button><a href="/users/sign-out">Sign Out</a></button>
		{{/if}}
		</p>
		{{{body}}}
	</body>
</HTML>
